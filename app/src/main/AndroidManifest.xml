<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.minhasplantinhas">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MinhasPlantinhas"
        tools:targetApi="31">
        <activity
            android:name=".DetalhesPlanta"
            android:exported="false"
            android:label="@string/detalhes" />
        <activity
            android:name=".ListaDePlantas"
            android:exported="false"
            android:label="@string/todas_as_plantas" />
        <activity
            android:name=".MinhasPlantas"
            android:exported="false"
            android:label="@string/title_activity_minhas_plantas"
            android:theme="@style/Theme.MinhasPlantinhas.NoActionBar" />
        <activity
            android:name=".DetalhesMinhasPlantas"
            android:exported="false"
            android:label="@string/detalhes" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>